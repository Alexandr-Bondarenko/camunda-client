import { FormDefault } from './Form.Default'
import { StartForm } from './StartForm'

const formsList = [
    'clientData',
    'loanData',
    'loanApproved'
]

export { FormDefault, StartForm, formsList }
